# FuelLead — текущее состояние проекта

## Надёжность Mail.ru — 2026-09-10

- Проверены действующие пароли всех четырёх ящиков: SMTP и IMAP работают. С отдельного разрешения пользователя отправлено ровно четыре диагностических письма на `artel.work@mail.ru`: все найдены во «Входящих» по Message-ID, все копии сохранены в «Отправленных».
- Исправлены SMTP-формат CRLF/quoted-printable, классификация временной авторизации и отказов RCPT/DATA, сохранение безопасных ответов провайдера, обработка неопределённого исхода после TCP/TLS-обрыва. SMTP и IMAP получили независимые статусы; добавлена фоновая проверка временных ошибок вне активных кампаний, автоматическая проверка после замены пароля и защита от устаревшего результата проверки.
- IMAP: закрытие сокетов при неудачном входе, ограниченные повторы временных подключений, безопасная обработка ошибок чтения, фильтрация старых UID и BODY.PEEK[]. Добавлены пять диагностических полей миграцией `20260910_mail_health`.
- Проверки: **232 passed**, TypeScript/Vite build, Docker build и runtime API 200. Браузер: desktop/390/320 px, форма теста и отмена; исправлена ширина мобильных кнопок. Результаты и ограничения: [MAIL_RELIABILITY_AUDIT_2026-09-10.md](docs/MAIL_RELIABILITY_AUDIT_2026-09-10.md).
- Важно: ранее активная кампания №63 поставлена на паузу для обновления и тестов, **393 письма остаются в очереди**. Автоматически не возобновлялась. `OUTREACH_AUTOMATIC_SEND_ENABLED=false` сохранён. Компании, адреса, доставки и исключения совпали по контрольным суммам; счётчики ящиков сохранены.
- Обновлены локальные backend, imap-worker и frontend. Пароли/ключи не менялись; commit/push не выполнялись. Безопасная диагностика: `docker compose exec -T backend python -m app.commands.check_mailboxes --verify` — проверяет вход без отправки писем.

## Мобильное меню — 2026-09-10

- При ширине до 900 px вместо кнопки выхода в шапке — кнопка меню из двух полосок; открытое состояние обозначено крестиком. Разделы и подписанный выход перенесены в раскрывающуюся панель, внутри показан аккаунт. Шапка остаётся компактной, 69 px; настольная боковая панель сохранена.
- Закрытие по выбору раздела, повторному нажатию, Escape, клику снаружи и уходу фокуса; Escape возвращает фокус кнопке. Скрытые ссылки исключены из Tab-порядка. При переходе между мобильной и настольной шириной меню сбрасывается. Короткие экраны поддерживаются прокруткой панели.
- Проверки: TypeScript/Vite и Docker build; Chrome 320/390/768/900 px и desktop 1440 px, навигация, клавиатура, закрытие, смена ширины, reduced motion и переход на форму входа через выход. Выход в QA подменён ответом API; письма, поиск и бизнес-данные не изменялись. Артефакты: `/tmp/fuellead-burger-qa/`.
- Обновлён локальный Docker frontend через `--no-deps`. Изменены `AppSidebar.tsx`, стили мобильной навигации и эта запись. Commit/push не выполнялись.

## Дизайн в стиле Apple — 2026-09-10

- Сохранён основной зелёный `#176647`; нейтральная палитра: фон `#f5f5f7`, белые поверхности, графитовый текст `#1d1d1f`, вторичный текст `#62656b`, светло-зелёные акценты `#eaf3ee`. Белый текст на зелёном: контраст 6,94:1.
- Системная типографика, увеличенные отступы и показатели, карточки со скруглением 22 px, мягкие тени, полупрозрачная боковая панель, зелёный активный пункт, сегментированные фильтры и светлая страница входа. Полноэкранная анимация поиска убрана; карточка прогресса и её управление сохранены. Учитываются reduced motion, reduced transparency и повышенная контрастность.
- Изменения этой задачи: `frontend/src/styles.css`, выделение текста в `AuthPage.tsx` и эта запись. Предыдущие изменения темы, иконки и других файлов сохранены.
- TypeScript/Vite и Docker build прошли. Chrome: пять разделов с загруженными данными при 1440/1024/768/390/320 px, фильтры, столбцы, раскрытие компании, вход и навигация; без ошибок JS и переполнения страницы. Исправлен перенос длинных адресов/статусов ящиков при 320 px. Диалог при 1440/390/320 px: Escape, фокус с клавиатуры, без внутреннего переполнения. Восстановление и остановка поиска проверены на подменённых ответах API при 1440/390 px.
- Обновлён только локальный Docker frontend (`--no-deps`), `http://localhost:8080`. Backend, БД и imap-worker не пересоздавались. QA не отправляет письма, не запускает реальный поиск и не сохраняет настройки; API предпросмотра письма разрешён отдельно. Артефакты: `/tmp/fuellead-apple-qa/`. Commit/push не выполнялись.

## Зелёная тема и иконка — 2026-09-10

- Присланные пользователем стили адаптированы к текущим React-компонентам: фон `#f6f8f7`, акцент `#176647`, контрастные границы, типографика, кнопки, поля, таблицы, карточки, тепловая карта, вход и диалоги. Мобильная навигация сохранена доступной; на узких экранах основные действия расположены друг под другом.
- Создана и подключена иконка «капля со стрелкой роста»: `frontend/public/icons/`, favicon 32, Apple Touch 180, app icons 192/512 и web manifest. Исходник и описание генерации сохранены в проекте.
- Проверки: TypeScript/Vite production build; Chrome — все пять разделов при 1440/1024/768/390/320 px, вход, фильтры, столбцы, раскрытие компании и навигация. Диалог при 1440/390/320 px, закрытие Escape и фокус с клавиатуры. Панель поиска при 1440/390 px, восстановление после reload и остановка на подменённых ответах API. Ошибок JavaScript и горизонтального переполнения нет. Артефакты: `/tmp/fuellead-theme-20260910/`.
- Обновлён только локальный Docker frontend (`--no-deps`) на `http://localhost:8080`. Backend, БД и worker не пересоздавались; QA не запускал реальный поиск/рассылку и не изменял бизнес-данные. Commit/push не выполнялись.

## Новые компании и пустой регион Okvedo — 2026-09-10

- Реальная причина нулевых добавлений в запуске №119: в поисковой выдаче Okvedo явно указан регион «Москва», но у части тех же ИНН карточка содержит `region=null` и пустые адреса. Отсев известных ИНН уже работал; до исправления в БД было 6 627 компаний и 6 627 уникальных ИНН. Запуск мягко остановлен на 250 кандидатах с сохранением позиции.
- Okvedo сохраняет явно указанные регионы только текущей страницы ответа, по ИНН. Если карточка полностью лишена региона, города и адресов, используется регион из поискового ответа для того же ИНН. Адресные данные карточки имеют приоритет; регион не выводится из префикса ИНН или заданного фильтра. Без подтверждения региона компания не добавляется.
- В API и интерфейс добавлены `skipped_known`, `skipped_region`, `skipped_unknown_region`; недействующие компании учитываются прежним `skipped_inactive`. Поля добавляются идемпотентно без изменения компаний и старых запусков; исторические значения новых счётчиков остаются нулевыми. Подписи: «Кандидаты» и «Добавлено», отдельно причины пропуска.
- Проверки: **201 backend tests passed**, одно существующее предупреждение Starlette/httpx; production build frontend; `git diff --check`. Регрессия проходит 205 существующих фирм и одно исключение на нескольких страницах, добавляет четыре новых с пустым регионом карточки и при втором запуске не делает ни одного запроса карточки. Проверены несовпадающие ИНН, неизвестный регион, приоритет адреса/региона карточки и очистка кэша между страницами.
- Backend/frontend пересобраны и обновлены в локальном Docker после остановки поиска и проверки отсутствия активных SMTP-попыток. База данных и imap-worker не пересоздавались. Рассылка не запускалась; прежние незакоммиченные изменения сохранены.
- Browser QA: Chrome, 1440×1000 и 390×844, счётчики и причины пропуска, восстановление после reload, остановка; без ошибок консоли и горизонтального переполнения. В этой проверке подменялись только `/api/search-runs*`; тестовые запуски не записывались в БД. Скрипты, скриншоты и результаты: `/tmp/fuellead-discovery-20260910/`.
- Отдельно возобновлён реальный полный запуск **№120** с прежними ОКВЭД. На проверенном снимке: **28 новых**, 149 пропусков известных/исключённых, 32 запроса карточек, четыре пропуска без подтверждённого региона, ноль обновлений существующих компаний. Все три диагностические фирмы сохранены. БД: **6 655 компаний / 6 655 уникальных ИНН**. Поиск продолжает работу через Okvedo; Checko вернул `timeout`. Это промежуточный результат, завершение полного прохода не проверялось.
- Commit/push не выполнялись.

## Ротация почтовых ящиков — 2026-09-09

- Исправлена причина пропуска проверенных ящиков: `blocked_until_round` сохранялся от старой кампании и после успешной проверки подключения, хотя новая кампания начинает круги с 1.
- Успешная SMTP/IMAP-проверка снимает блокировку полностью. Новая подтверждённая кампания очищает старые номера блокировки только у проверенных ящиков своего снимка. Завершение временной блокировки также очищает маркер; новая ошибка по-прежнему блокирует три следующих круга текущей кампании.
- Очередность сохраняется: по одной пачке каждого доступного ящика, затем общий отдых. Суточные счётчики, лимиты и прогрев не сбрасываются.
- Проверки: **188 backend tests passed**, существующее предупреждение Starlette/httpx. Регрессии до исправления давали 5 ошибок; проверены три круга 5/5/6 писем с каждого ящика, уникальность получателей, перерывы и восстановление после ошибки. После дополнительных проверок счётчиков повторно прошли все 17 тестов рассылки.
- Локальный backend обновлён во время сохранённого перерыва, без активного поиска и SMTP-попыток. В работающей версии изменены только `outreach.py` и `sender_accounts.py`; пакеты и остальные файлы приложения совпадают с предыдущим контейнером. Существовавшие различия рабочего репозитория и контейнера вне этого исправления сохранены. Код развёртываемой версии отдельно прошёл 27 почтовых тестов; один тест другой, ранее не развёрнутой функции повторной отправки исключён.
- Все четыре реальных ящика успешно прошли SMTP/IMAP-проверку подключения, `verification_status=verified`, `blocked_until_round=NULL`. Тестовых писем не отправляли. Автоматическая отправка остаётся выключенной; ранее подтверждённая кампания №63 сохраняет свой график.
- Сравнение до/после: кампания №63, круг 9, очередь 468, история результатов, счётчики ящиков, лимиты, прогрев и окончание перерыва **09.09.2026 17:44:18 МСК** сохранены. Фронтенд HTTP 200, защищённый API без авторизации HTTP 401. Ротация подтверждена изолированными тестами; следующий реальный круг ещё не наблюдался.
- Commit/push не выполнялись. Пользовательский каталог `тг пост/` сохранён.

Дата проверки интеграции API: 2026-09-04

Рабочая копия: `/Users/mustafa/Documents/Codex/2026-08-27/fuellead-project-context-and-mvp-requirements/FuelLead`

Ветка: `main`

Базовый HEAD перед интеграцией API: `f91ca02`.

## Полный поиск одним нажатием — 2026-09-04

- Причины маленьких запусков: локальный `DISCOVERY_LIMIT_PER_CODE=2`, максимум две страницы на ОКВЭД/регион, лимиты 5 search/5 egr у API-ФНС. В последних запусках №112–113 также были недоступные ключи Checko, ограничение частоты Okvedo и отсутствующая головная карточка DaData.
- `POST /api/search-runs` по умолчанию использует `search_scope=full`. Интерфейс явно отправляет этот режим и показывает «Запустить полный поиск». Старая порция доступна только как `search_scope=batch`.
- Полный режим обходит страницы с сохранённой позиции до конца доступной выдачи или лимита провайдера. Ограничения числа кандидатов, двух страниц и 5/5 запросов не применяются. Размер страницы Checko/Okvedo/API-ФНС — 100, DaData — 20.
- Сохранены порядок Checko → Okvedo → DaData → API-ФНС и прежнее условие резерва: подтверждённое исчерпание суточных квот каждого основного источника. Конец выдачи и ошибки доступа не считаются исчерпанием.
- Между вызовами клиента — 1,05 секунды. Временное ограничение частоты повторяется до трёх раз с ожиданием 60/120/180 секунд; для Okvedo учитывается больший `Retry-After`. Ожидание можно остановить.
- Отсутствующая карточка DaData больше не блокирует остальные ИНН. Повторение провайдером одной страницы/содержимого ограничено; результат и позиция сохраняются.
- Добавлены прогресс по источнику/ОКВЭД/региону/странице, счётчики вызовов поиска/карточек, итоговые причины каждого этапа, кнопка остановки. Счётчики не включают внутреннюю ротацию ключей Checko и служебную статистику.
- `GET /api/search-runs/latest` восстанавливает поиск после перезагрузки страницы; polling возобновляется после временной ошибки сети. Повторное нажатие возвращает активный запуск, PostgreSQL advisory lock сериализует создание.
- `POST /api/search-runs/{id}/stop` выставляет флаг. Текущий ответ сохраняется, следующие запросы не начинаются. Статус `cancelled`, найденные компании и курсоры сохраняются.
- Идемпотентная миграция добавляет поля к search_runs, старые записи получают `batch`. Работа в фоне требует включённого backend/компьютера. Перезапуск backend помечает поиск прерванным, следующий ручной запуск использует сохранённые курсоры. Ежедневное расписание не включалось.

Проверка:

- Backend: **180 passed**, одно существующее предупреждение Starlette/httpx. Новые сценарии: 350 компаний на четырёх страницах, 650 карточек/8 поисковых вызовов API-ФНС, известные/исключённые ИНН, точное продолжение после квоты, остановка во время запроса, повторы ограничения частоты, отсутствующая карточка, повтор страницы, повторный клик и миграция.
- TypeScript/Vite и Docker production builds успешны; `git diff --check` без ошибок.
- Docker backend/frontend пересобраны и пересозданы. Миграция проверена на PostgreSQL. DB и imap-worker не пересоздавались; автоматическая отправка остаётся выключенной, SMTP в момент обновления не выполнялся.
- Browser QA: `http://localhost:8080/#companies`, Playwright + Chrome; Browser plugin отсутствует. Проверены 1440×1000 и 390×844: запуск full, счётчики, восстановление после reload без второго запуска, остановка и сохранённый результат. Страница/заголовок корректны, нет пустого экрана, overlay ошибок, ошибок консоли и горизонтального переполнения. Исправлены кликабельность карточки и ширина мобильной кнопки остановки.
- Только `/api/search-runs*` в browser QA подменялись; авторизация и отображаемые компании реальные. Тестовый запуск №987654 не записывался в БД. Скриншоты и скрипт: `/tmp/fuellead-full-search-qa/`.
- После обновления в рабочем приложении появился отдельный реальный запуск **№114**, `full`. На последнем снимке: **74 новых компании**, 95 кандидатов, 3 поисковых вызова/95 запросов карточек; Okvedo, ОКВЭД 42.11, регион 77, страница 2. Запуск продолжает работу. Checko вернул `keys_unavailable`; полной выработки квот и завершения всех этапов этот снимок не доказывает. Реальный запуск не останавливался проверками.
- Предыдущие незакоммиченные изменения интеграции сохранены. Commit/push не выполнялись.

## Okvedo, DaData и последний резерв API-ФНС — 2026-09-04

- Подключены Okvedo и DaData. Ключи из пользовательского файла сохранены только в игнорируемом `.env`; `.env.example` содержит пустые поля.
- `DISCOVERY_PROVIDER=combined`: Checko → Okvedo → DaData → API-ФНС. Последний этап разрешён только после подтверждённого исчерпания суточных квот каждого настроенного основного источника.
- Пустой поиск, лимит порции, HTTP 401/403 без подтверждения квоты, таймаут и временный HTTP 429 не открывают резерв.
- Checko проверяет все ключи отдельно для `search` и `company`; причина исчерпания передаётся в оркестратор, включая случай нескольких ключей.
- DaData после 403 проверяет сегодняшний `remaining.suggestions` через `/api/v2/stat/daily`; при недоступной/неоднозначной статистике API-ФНС не вызывается. `X-Secret` используется только для статистики.
- В `auto` и при явном выборе `api_fns` основные настроенные источники нельзя обойти; при единственном настроенном API-ФНС работает самостоятельно.
- Для новых провайдеров сохранены отдельные курсоры, отсев известных/исключённых ИНН, активность и проверка текущего региона карточки (77/50). Прерывание после частичного результата сохраняет компании и позицию повторной попытки.
- DaData — до 20 подсказок на ОКВЭД/регион, без пагинации и гарантии новых результатов после обработки порции. Контакты зависят от тарифа; в проверочной карточке DaData контактов не было.
- Обновлены API `/api/health`, типы и подписи интерфейса: «Несколько API · ФНС в резерве» и объяснение условия запуска резерва.

Проверка:

- Backend: **169 passed**, одно существующее предупреждение Starlette/httpx.
- Frontend: `npm run build` успешно (TypeScript + Vite); `git diff --check` без ошибок.
- Реальные ограниченные проверки: Okvedo — 2 поиска и 2 карточки; DaData — 2 поиска и 1 карточка, плюс 1 статистика. Ответы успешны; текущий регион обеих проверочных карточек — 77. Эти проверки не записывали компании в рабочую БД.
- Проверочная статистика DaData на 2026-09-04: использовано 3 запроса подсказок, осталось 9997. Это снимок, не текущий постоянный остаток.
- API-ФНС и Checko в реальной сети для этой задачи не вызывались. Переключения квот проверены изолированными тестами.
- Локальный Docker backend/frontend обновлён и пересоздан. Обычная Compose-сборка остановилась на таймауте Docker Hub; использованы новые локальные слои поверх существующих образов с установленными зависимостями. Состав зависимостей не менялся; frontend использует проверенный production bundle.
- Runtime подтвердил `combined`, все четыре провайдера и `OUTREACH_AUTOMATIC_SEND_ENABLED=false`.
- Browser QA: `http://localhost:8080/#companies`, Playwright + установленный Chrome (Browser skill отсутствует), 1440×1000 и 390×844. Страница не пустая, без framework overlay, ошибок консоли и горизонтального переполнения. Проверены новая подпись, кнопка поиска, состояние выполнения и завершение. Только search-run запросы в браузере были подменены тестовыми ответами; авторизация, конфигурация и существующие данные реальные.
- Скриншоты сохранены вне репозитория в outputs текущей задачи. Пользовательская БД, рассылка и история поиска не изменялись проверками.
- Commit/push не выполнялись.

## Предыдущий снимок почтовой интеграции — 2026-09-03

Ниже сохранены результаты предыдущей работы. Состав ящиков и другие данные этого снимка могут отличаться от текущего состояния.

## Безопасное состояние запуска

- `OUTREACH_AUTOMATIC_SEND_ENABLED=false` в локальном `.env` и `.env.example`.
- Значение не переключалось на `true`.
- `MAIL_CREDENTIALS_ENCRYPTION_KEY` в локальном `.env` пока не задан. До добавления ящиков нужно сгенерировать ключ по инструкции ниже.
- В `sender_accounts` сейчас нет тестовых или реальных ящиков.
- Реальные SMTP/IMAP-подключения и отправка писем в ходе реализации и QA не выполнялись.
- Старые или ранее переданные пароли не использовались и не сохранялись.

## Реализовано

### Mail.ru и безопасное хранение

- Gmail API-клиент, Gmail CLI-команда и Gmail-тесты удалены.
- Добавлены ящики Mail.ru с фиксированными безопасными параметрами:
  - SMTP `smtp.mail.ru:465`, implicit SSL/TLS, обязательная авторизация полным email;
  - IMAP `imap.mail.ru:993`, SSL/TLS.
- Пароль внешнего приложения шифруется Fernet-ключом из `MAIL_CREDENTIALS_ENCRYPTION_KEY`.
- API и frontend никогда не возвращают `password` или `encrypted_password`; после сохранения показывается только «Пароль сохранён».
- Ошибки SMTP/IMAP очищаются от секрета, AUTH-команд, токенов и содержимого письма.
- Добавлена безопасная CLI-команда генерации ключа.
- Временные ошибки SMTP-подключения повторяются до трёх раз до начала отправки.
- После принятия письма SMTP-сервером приложение сохраняет ту же копию в системной папке
  «Отправленные» через IMAP и отдельно сообщает результат сохранения, не меняя подтверждённый
  статус SMTP при ошибке IMAP.

### Управление ящиками

- Отдельная страница «Почтовые ящики» позволяет:
  - добавить Mail.ru-ящик;
  - задать отображаемое имя, пароль внешнего приложения и дневной лимит;
  - включить/отключить SMTP и IMAP;
  - проверить SMTP-авторизацию без отправки;
  - отправить ровно одно тестовое письмо на вручную введённый и подтверждённый адрес;
  - приостановить/активировать ящик;
  - заменить пароль;
  - удалить неиспользуемый активной кампанией ящик после подтверждения.
- Карточка показывает состояние подключения, активность, последнюю проверку, прогрев, лимит, отправки сегодня, последнюю отправку и блокировку.

### Многопочтовый планировщик

- Одновременно допускается только одна активная кампания.
- Перед запуском создаётся снимок темы, текста, получателей, проверенных активных ящиков, лимитов и интервалов; подтверждение действительно 10 минут.
- Отправка последовательная: один ящик выполняет одну пачку, затем следующий; после полного круга сохраняется общий отдых.
- Интервал между письмами выбирается один раз в диапазоне 60–85 секунд и хранится в PostgreSQL.
- Отдых после круга выбирается один раз в диапазоне 77–93 минут и хранится в PostgreSQL.
- Размер пачки хранится отдельно для каждого ящика и растёт от 5 до 12 только после полностью успешной пачки без ошибок.
- Прогрев ящика сохраняется между кампаниями; дневной лимит имеет приоритет.
- Проблемный ящик прекращает текущую пачку и блокируется на три следующих полных круга; незапущенные письма остаются в общей очереди.
- PostgreSQL row locks и атомарный `claim_token` защищают одно письмо от двух worker-процессов.
- Перед каждым SMTP-вызовом повторно проверяются компания, её активность/статус/email, глобальное исключение, отсутствие предыдущей отправки и допустимый статус кампании.
- Нарушение проверки даёт `suppressed` без SMTP-вызова.
- Обрыв после начала SMTP-попытки без доказуемого ответа даёт `uncertain`, запрещает автоповтор и переводит кампанию в `interrupted`.
- Пауза запрещает новые SMTP-попытки, но сохраняет таймеры. Необратимая остановка переводит оставшиеся `queued` в `cancelled`.

### Статусы и результаты

- Delivery: `queued`, `sending`, `accepted`, `failed`, `bounced`, `uncertain`, `suppressed`, `cancelled`.
- Campaign: `draft`, `running`, `paused`, `cooldown`, `interrupted`, `completed`, `stopped`.
- `accepted` означает только принятие сервером после DATA, а не подтверждённую доставку или попадание во «Входящие»; это явно написано в интерфейсе.
- Для принятого письма сохраняются безопасный SMTP response, Message-ID, sender account и время.
- Постоянный адресный отказ классифицируется по SMTP/DSN-коду, а не только по строке `550 5.1.1`.

### Глобальные исключения и IMAP

- Добавлена страница «Исключения»: просмотр, поиск, ручное добавление и осторожное снятие.
- Исключения проверяются при создании снимка и прямо перед SMTP-вызовом.
- Отдельный Compose-сервис `imap-worker` читает новые DSN по UID только для активных ящиков с включённым IMAP.
- DSN сопоставляется по Message-ID и техническим заголовкам; поздний bounce переводит `accepted` в `bounced` и добавляет email в глобальные исключения.
- Обработка идемпотентна; позиция UID сохраняется. Нераспознанные письма не удаляются и полностью не логируются.

### Наблюдаемость

- Диалог кампании показывает круг, активный ящик, позицию в пачке, следующий запуск, окончание отдыха и счётчики всех delivery-статусов.
- Логи отправки структурированы и используют идентификаторы/безопасные коды без паролей, токенов и текста писем.

## PostgreSQL-миграция

Файл: `backend/app/migrations/20260903_mailru_scheduler.sql`.

Миграция:

1. Создаёт `schema_migrations` и регистрирует версию `20260903_mailru_scheduler`.
2. Создаёт `sender_accounts` с уникальным нормализованным `email`, зашифрованным паролем, SMTP/IMAP-настройками, verification-данными, дневным лимитом, прогревом, счётчиками и блокировкой.
3. Создаёт `email_suppressions` с причиной, источником, campaign/delivery, SMTP/DSN-кодом, датами и комментарием.
4. Создаёт `imap_processed_messages` для идемпотентной обработки возвратов.
5. Расширяет `outreach_campaigns` полями снимка и сохранённого состояния планировщика.
6. Расширяет `outreach_deliveries` полями sender account, SMTP response/code, Message-ID, claim token и attempt timestamps.
7. Переводит старый delivery-статус `sent` в `accepted`.
8. Переводит старый campaign-статус `cancelled` в `stopped`; небезопасные старые активные кампании без sender snapshot становятся `interrupted` без автоматического повтора.
9. Пересоздаёт PostgreSQL CHECK constraints после преобразования данных и добавляет необходимые индексы.

Проверено на реальном локальном PostgreSQL после миграции:

- версия миграции присутствует в ledger;
- campaign CHECK содержит ровно `draft/running/paused/cooldown/interrupted/completed/stopped`;
- delivery CHECK содержит ровно `queued/sending/accepted/failed/bounced/uncertain/suppressed/cancelled`;
- существующая история сохранена: `accepted=314`, `cancelled=691`, кампании `completed=1`, `stopped=6`;
- после browser QA тестовый ящик удалён, `sender_accounts=0`.

## Проверки

- Backend: `98 passed, 1 warning` (`Starlette TestClient/httpx` deprecation), 0.97 s.
- Frontend: `npm run build` успешно; TypeScript и Vite прошли, 1755 modules transformed.
- Docker frontend image: production build успешен.
- `docker compose config --quiet`: успешно.
- Compose: `db`, `backend`, `imap-worker`, `frontend` запущены; PostgreSQL healthy.
- Runtime после возврата к локальному `.env`: `automatic_send_enabled=False`, `encryption_key_configured=False`.

Browser QA:

- desktop `1440×1000`: страница ящиков отображается полностью, `scrollWidth=1440`, ошибок/warnings консоли нет;
- mobile `390×844`: ящики и исключения без горизонтального переполнения, `scrollWidth=390`; навигация видна в две строки, действия карточки доступны;
- тестовый ящик с локальными фиктивными данными был добавлен без SMTP/IMAP-проверки и затем удалён;
- после сохранения поле пароля очищалось, на странице отображался только признак «Пароль сохранён»;
- пароль не появился в тексте страницы и не сохранялся frontend-кодом в localStorage/sessionStorage;
- серверная ошибка для чужого домена отображалась понятно: «Укажите полный адрес ящика Mail.ru» — без секрета.

## Что осталось непроверенным намеренно

- Реальная авторизация новых паролей внешнего приложения на `smtp.mail.ru:465`.
- Реальная отправка одного ручного тестового письма.
- Реальное чтение DSN через `imap.mail.ru:993` и поздний bounce от Mail.ru.
- Фактическая доставка письма и попадание во «Входящие».

Эти проверки требуют трёх новых локально введённых паролей и осознанного ручного действия пользователя. Автотесты используют изолированные fake-транспорты и не подключаются к Mail.ru.

## Как локально подключить три ящика

1. Сгенерировать Fernet-ключ без сохранения в репозиторий:

   ```bash
   docker compose run --rm --no-deps backend python -m app.commands.generate_mail_credentials_key
   ```

2. Вставить выведенный ключ только в игнорируемый локальный `.env`:

   ```dotenv
   MAIL_CREDENTIALS_ENCRYPTION_KEY=...
   OUTREACH_AUTOMATIC_SEND_ENABLED=false
   ```

3. Пересоздать сервисы, чтобы они прочитали новый `.env`:

   ```bash
   docker compose up -d --build --force-recreate backend imap-worker
   docker compose restart frontend
   ```

4. Открыть `http://localhost:8080/#mailboxes` → «Почтовые ящики».
5. По очереди добавить:
   - `artel.work@mail.ru`
   - `mustafa.tojiev.2005@mail.ru`
   - `artel.office8@mail.ru`
6. Для каждого вставить новый пароль внешнего приложения именно в поле «Пароль внешнего приложения», задать лимит и нажать «Добавить ящик».
7. Нажать «Проверить» у каждого ящика. Эта кнопка авторизуется по SMTP, но письмо не отправляет.
8. IMAP включать только там, где нужен сбор возвратов. «Тестовое письмо» использовать отдельно и только с вручную введённым подтверждённым адресом.

Пароли ящиков нельзя помещать в `.env`, коммитить, отправлять в чат или повторно использовать после компрометации.
