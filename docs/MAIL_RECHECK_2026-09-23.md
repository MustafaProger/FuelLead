# Повторная проверка почты — 23.09.2026

Проверено на рабочем сервере 130.49.151.239 около 10:00 МСК.

- Все шесть активных ящиков успешно прошли SMTP AUTH и IMAP с TLS. Эти проверки не отправляли письма и не изменяли сохранённые статусы.
- Контейнеры работали, PostgreSQL healthy, свободно около 69 ГБ.
- В ящиках 5, 6, 7 сохранены те же отказы `DATA 550 spam message rejected`, что и 22 сентября. Новые попытки отправки не обнаружены; успешный вход не доказывает снятие отказа на содержимое письма.
- Кампания 153 уже остановлена пользователем: 11 accepted, 5 failed, 484 cancelled. Это новое состояние относительно вчерашней паузы; его не изменяли. Активных кампаний, отправляющихся писем и ответов нет.
- Ошибка интерфейса: все `sent_today_date` равны 2026-09-22, но API возвращал эти значения под подписью «Сегодня» 23 сентября. Чтение теперь вычисляет сегодняшний расход в часовом поясе приложения без сброса хранимых счётчиков. Неизвестная и будущая даты сохраняются консервативно.
- Результат проверки ящика с сохранённым антиспам-отказом теперь поясняет, что это прежний отказ отправки. Он не заявляет, что передача письма проверена, и не стирает отказ.
- Проверки: 717 passed, 13 skipped; frontend build и git diff --check успешны.
- Резервная копия исходников перед изменениями: `/root/fuellead-mail-repair-20260923/source-before.tgz`.
- Изменения развёрнуты в backend, IMAP worker и frontend. Авторизованный health API вернул 200; API ящиков показывает сегодняшний расход 0 для всех шести ящиков. Новый текст пояснения найден в опубликованном JS.
- После выкладки подтверждено: хранимые счётчики за 22 сентября и три антиспам-отказа не изменились; кампания 153 остаётся stopped с 11 accepted, 5 failed, 484 cancelled. Новая рассылка не запущена.

## Три разрешённых теста через браузер

После разрешения пользователя «используй плагин зайти на вкладку и сделай» через открытую вкладку Chrome выполнено по одной штатной операции «Тестовое письмо» для ящиков 5, 6, 7. Получатель всех трёх — artel.mos@gmail.com. Использовано стандартное короткое тестовое письмо интерфейса с темой «FuelLead — проверка почты SMTP».

Во всех трёх случаях интерфейс подтвердил принятие SMTP и сохранение копии в «Отправленных». Журнал backend подтвердил ровно три успешных запроса test-email (по одному на каждый ящик). Независимым чтением заголовков из IMAP Sent подтверждены:

| Отправитель | Время МСК | Message-ID |
|---|---|---|
| mustafa.tojiev@mail.ru | 23.09.2026 10:16:10 | `<179014777063.1.2101534441044681073@mail.ru>` |
| artel.email@mail.ru | 23.09.2026 10:16:39 | `<179014779918.1.8418377570599247932@mail.ru>` |
| artel.moss@mail.ru | 23.09.2026 10:17:09 | `<179014782962.1.10101766814977887714@mail.ru>` |

Обнаружено, что прежний обработчик теста возвращал успех, но не обновлял состояние ящика. Для этих трёх подтверждённых результатов выполнено однократное восстановление статуса verified и снятие старых ограничений после сверки исходной ошибки и отсутствия более новой проверки. Аудит до/после и доказательства: `/root/fuellead-test-recovery-20260923/reconciliation.json`; резервная копия БД: `/root/fuellead-test-recovery-20260923/database-before.dump`. Повторных тестовых писем для обновления статуса не отправляли.

В пользовательской вкладке после обновления все шесть ящиков показали «SMTP подключён». Кампания 153 осталась stopped; отменённые адресаты не восстановлены, лимиты и счётчики не менялись. Принятие этих коротких тестов не доказывает доставку во «Входящие» Gmail и не гарантирует принятие прежнего шаблона рассылки.

Постоянное исправление: обработчик тестового письма теперь сохраняет verified после фактического SMTP-принятия, если настройки подключения и SMTP-состояние не изменились за время запроса. Неуспешная или неопределённая отправка не снимает ограничение; параллельный IMAP не отменяет успешный результат. Тесты после изменения: 731 passed, 13 skipped; git diff --check успешен.
