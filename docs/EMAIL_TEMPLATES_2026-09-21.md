# Шаблоны коммерческих предложений — 21 сентября 2026

Реализованы и установлены на `130.49.151.239`, `/opt/fuellead`:

- Переключение обычного текста и HTML, отдельные сохраняемые редакторы, предпросмотр и скачивание HTML-шаблона.
- Подстановка компании, даты, ИНН, ОКВЭД и адреса получателя. В HTML значения экранируются, активное содержимое удаляется с помощью nh3. Предпросмотр изолирован sandbox/CSP; внешние изображения в нём не загружаются.
- До пяти вложений общим размером 10 МиБ. Защищённые загрузка и скачивание; неизменяемые файлы хранятся в PostgreSQL, поэтому сохраняются при пересоздании контейнеров.
- SMTP формирует текстовую и HTML-части вместе с вложениями; в IMAP сохраняется тот же MIME. Одиночная отправка и новые кампании используют общий шаблон. Снимки кампаний сохраняют готовый HTML и ссылки на файлы, независимо от дальнейших правок шаблона.
- Для существующих шаблонов, кампаний и доставок добавлены поля с совместимыми значениями по умолчанию. Проверена повторная миграция SQLite и PostgreSQL.

Основным шаблоном на сервере сохранено HTML-предложение АРТЭЛЬ из текста, присланного пользователем в переписке 21 сентября: ЛУКОЙЛ и Teboil, ДТ — наценка 1%, бензин — 2%, Мустафа, 8 964 517-78-08, Telegram @mustafa_proger. Текст документа для ООО СТК с маслами и антифризом заменён уточнением пользователя и в рассылку не включён. Загруженные вложения не персонализируются внутри файла.

Проверки:

- Локально: 670 backend-тестов прошли, 13 вариантов PostgreSQL пропущены; 4 frontend-теста и TypeScript/Vite build прошли.
- В собранном серверном образе: все 683 backend-теста прошли, включая отдельную временную PostgreSQL без доступа к рабочей базе; дополнительно проверена аддитивная миграция старых строк и её повторное выполнение. Временный контейнер PostgreSQL удалён после проверки.
- Браузер: переключение и сохранение форматов, загрузка/скачивание/удаление файла, ошибки переменных, восстановление заготовки; ширины 320, 390, 768 и 1440, без горизонтального переполнения страницы и письма. Проверен интерфейс развернутого приложения после авторизации через SSH-туннель.
- Production: обновлены backend, frontend и imap-worker, подтверждены авторизованные health/template/preview API и персонализация на реальной компании. Прямой HTTP-запрос к публичному IP вернул 200. 21 файл релиза сверены по SHA-256.
- Контрольные данные компаний, статусов доставок, всех кампаний и количества исключений до/после совпали. Активных кампаний и доставок `sending` нет, автоматическая отправка выключена. Реальные письма не отправлялись; попадание нового HTML-письма во входящие не проверялось.
- Резервная копия базы, исходников и контрольного состояния: `/opt/fuellead/backups/email-content-20260921T065825Z/`. Предыдущие образы также записаны в `image-ids.txt` в этой папке.

Локальный HTML-файл для пользователя: `/Users/mustafa/Downloads/Коммерческое предложение АРТЭЛЬ — шаблон.html`.

Изменения развёрнуты адресным копированием исходников и пересборкой Compose; Git commit/push в рамках этого запроса не выполнялись.

## Фирменная шапка — уточнение 21 сентября

В основной HTML-шаблон на сервере и заготовку АРТЭЛЬ добавлена оригинальная шапка из `word/media/image1.jpeg` предоставленного DOCX: логотип и голубая лента, без перерисовки. В предпросмотре и скачиваемом HTML изображение встроено как data URI; перед SMTP заменяется на CID с отдельной inline-частью `multipart/related`. Разрешён только этот встроенный JPEG, произвольные data URL удаляются. Основной текст, тема, формат и выбранные вложения при обновлении сохранены.

Проверены 318 тестов почтового содержимого, SMTP и рассылки локально и в серверном образе, включая исходные байты изображения, CID и IMAP-копию с обычным вложением и без него. Изображение загружается в production-предпросмотре (1786 × 241); адаптивность проверена на 320, 390 и 900 px. Статусы кампаний и доставок до/после совпали, реальные письма не отправлялись. Резервная копия шаблона и затронутых исходников: `/opt/fuellead/backups/letterhead-20260921T072452Z/`. Локальный HTML в Downloads также обновлён.
